  <div class="container" style="margin: auto; width: 50%; text-align: center">
    <div>
      <h1 style="margin-left: 30px">☙ Ingredients ❧</h1>
    </div>
    <div class="ingredients-list">
      <ul>
        <% @ingredients.each do |ingredient| %>
          <li style="list-style: none; color: white; font-weight: bolder" > <%= ingredient.name %> </li>
        <% end %>
      </ul>
    </div>
  </div>

